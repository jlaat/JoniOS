import React from "react";

const Guestbook = (props: any) => {
  return (
    <div className="guestbook" style={{ zIndex: props.zindex }}>
      Guestbook
    </div>
  );
};

export default Guestbook;
